def sorted_array_in_place(nums):
    if len(nums)==0:
        return 1
    i = 0
    for j in range(1,len(nums)):
        if nums[j] != nums[i]:
            i+=1
            nums[i]=nums[j]

    return i+1
sorted_array_in_place(arr)
